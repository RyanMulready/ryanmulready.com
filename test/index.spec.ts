import { describe, test } from 'vitest';
import { setup } from '@nuxt/test-utils-edge';

describe('test', async () => {
    await setup({
        // test context options
    });

    test('test', () => {
        // ...
    });
});
